        SimGenerationServiceIMPL.DataHolder<List<LoadEsimExcelInputDTO>> loadEsimExcelInputDTODataHolder = new SimGenerationServiceIMPL.DataHolder<>();
                List<LoadEsimExcelInputDTO> listOfDTO =  new ArrayList<>();
                uniqueSimDetailsDTO.forEach(i -> {
                    LoadEsimExcelInputDTO dto = new LoadEsimExcelInputDTO();
                    dto.setSerialNumber(i.getSimId());
                    dto.setImsi(i.getImsiId());
                    listOfDTO.add(dto);
                });

                loadESimData.checkAndInsert(listOfDTO, env, brandId, true, loadEsimExcelInputDTODataHolder::setData);

                if(!loadEsimExcelInputDTODataHolder.getData().isEmpty()) {
                    loadEsimExcelInputDTODataHolder.getData().forEach(i -> {
                        uniqueSimDetailsDTO.forEach(k -> {
                            if(k.getSimId().equalsIgnoreCase(i.getSerialNumber())) {
                                uniqueSimDetailsDTO.remove(k);
                            }
                            if(k.getImsiId().equalsIgnoreCase(i.getImsi())) {
                                uniqueSimDetailsDTO.remove(k);
                            }
                        });
                    });
                }
